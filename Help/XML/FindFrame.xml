<?xml version="1.0" encoding="utf-8" ?>
<?xml-stylesheet type="text/xsl" href="..\XSL\topic.xsl"?>

<topic name="FindFrame" type="method">
	<title>
		Open Twebst Library | Documentation - FindFrame Method
	</title>

	<description>
		Searches inside a container object, an HTML frame or iframe that verifies a list of <reference href="SearchCondition.htm">Search Conditions</reference>.
	</description>

	<apply>
		<apply_item>Browser</apply_item>
		<apply_item>Frame</apply_item>
	</apply>

	<call>
		var f = obj.FindFrame(Search conditions);
	</call>

	<arguments>
		<arg_summary>
			This method accepts a variable number of <reference href="SearchCondition.htm">Search Condition</reference> arguments.
			If no search condition is provided, then the first frame in the container is returned.
			Any HTML <i>frame</i> or <i>iframe</i> attribute is accepted in search conditions.
			A special zero-based <i>index</i> attribute is allowed in search condition specifying the ordinal position in the collection of objects generated by the remaining search conditions.
		</arg_summary>
	</arguments>

	<remarks>
		The method returns a web automation Frame object that matches the search condition arguments and provides programmatic control over the underlying <a target="_blank" href="http://msdn.microsoft.com/en-us/library/aa741505(VS.85).aspx">IHTMLWindow2</a> native object exposed by IE browser.
		If no frame verifies the search conditions, then <i>null</i> is returned.<br/>
		In case of an error the method throws a script exception and the lastError property of the core object is set as follows:
		<ul>
			<li>INVALID_ARG_ERROR: if invalid parameters are provided</li>
			<li>BROWSER_CONNECTION_LOST_ERROR: if the browser was closed.</li>
			<li>FAIL_ERROR: in case of anohter error.</li>
		</ul>
		<br/>
		The time allocated for search is specified by the searchTimeout property.
		If loadTimeout is not zero, then the method waits the browser objects to be completely loaded in the amount of time specified by the property.
		If the timeout has expired the lastError property is set to LOAD_TIMEOUT_ERROR. In this case, if loadTimeoutIsError property is true,
		then a script exception is thrown.
		<br/><br/>
		If the container object is a Browser, then the method searches the frame in all the &lt;frame&gt; and &lt;iframe&gt; hierarchy of the browser.
		If the container object is a Frame, then the method searches the frame in all the &lt;frame&gt; and &lt;iframe&gt; hierarchy of the container frame.
	</remarks>

	<example>
		<jscode>
var coreObj    = new ActiveXObject("OpenTwebst.Core");
var browser    = coreObj.StartBrowser("http://codecentrix.blogspot.com/");
var firstFrame = browser.FindFrame("");
firstFrame.nativeFrame.navigate("http://www.codecentrix.com");
		</jscode>
	</example>

	<seealso>
		<seealso_item>Frame</seealso_item>
		<seealso_item>FindChildFrame</seealso_item>
		<seealso_item>loadTimeout</seealso_item>
		<seealso_item>searchTimeout</seealso_item>
	</seealso>
</topic>
